const PasswordConst = {
  MIN: 6,
  MAX: 20,
};

const Role = {
  USER: 'user',
  ADMIN: 'admin',
};

const SuccessMessage = {
  logoutUser: 'Logged out successfully',
};

export { PasswordConst, Role, SuccessMessage };
